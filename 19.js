//  how can we access dynamically an object property based on a variable value

const user = {
    name : "Amit",
    age : 23
}

let key = 'name';

console.log(user[key]);
let key2 = "age";

console.log(user[key2])